import React from 'react';

const LoggingDefaultMessage = () => {
    return (
        <div>
            <h1>Front Matter</h1>
            <p>PropTypes also exports the following validators for
                ensuring that the value passed to a prop can be rendered by React. </p>
        </div>
    );
};

export default LoggingDefaultMessage;